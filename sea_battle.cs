void ShowSeaBattleField() // Функція для відображення поля морського бою
{
    // int size = 9; // Встановлюємо розмір поля 10x10
    // string letters = "ABCDEFGHIJ"; // Букви для підпису стовпців знизу поля
    // int shipCells = 0; // Лічильник клітинок з кораблями
    // string[,] field = new string[size, size]; // Створюємо двовимірний масив для поля

    // for (int i = 0; i < size; i++) // Проходимо по всіх рядках поля
    // {
    //     for (int j = 0; j < size; j++) // Проходимо по всіх стовпцях поля
    //     {
    //         field[i, j] = "~"; // Заповнюємо кожну клітинку хвилею (вода)
    //     }
    // }
    

    for (int j = 1; j <= 4; j++) // Розміщуємо горизонтальний корабель з 4 клітинок у рядку 2
    {
        field[8, j] = "x"; // Ставимо частину корабля (рядок 2 = індекс 8)
    }

    for (int i = 2; i <= 4; i++) // Розміщуємо вертикальний корабель з 3 клітинок у стовпці H
    {
        field[i, 7] = "x"; // Ставимо частину корабля (стовпець H = індекс 7)
    }

    field[5, 3] = "x"; // Ставимо однопалубний корабель (перший)
    field[6, 8] = "x"; // Ставимо однопалубний корабель (другий)


    for (int i = 0; i < size; i++) // Проходимо по всіх рядках для виводу
    {
       Console.Write((size - i) + " "); // Нумерація рядків зліва від 10 до 1
        for (int j = 0; j < size; j++) // Проходимо по всіх стовпцях для виводу
        {
            Console.Write(field[i, j] + " "); // Виводимо вміст клітинки з пробілом
            if (field[i, j] == "x") shipCells++; // Рахуємо клітинки з кораблями
        }
        Console.WriteLine(); // Переходимо на новий рядок після виведення повного рядка поля
    }

    Console.Write(" "); // Відступ під нумерацію рядків
     // Console.Write((size - i).ToString().PadLeft(2) + " "); - якщо 10х10
    foreach (char letter in letters) // Виводимо букви A–J знизу поля
    {
        Console.Write(letter + " ");
    }
    Console.WriteLine();

    Console.WriteLine($"Кораблів на полі: {shipCells} клітинок"); // Виводимо підсумок
}

ShowSeaBattleField(); // Викликаємо функцію для відображення поля
