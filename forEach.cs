using System;
class Program
{
    static void Main()
    {
        // масив чисел заданий одразу
        int[] arr = { 3, 7, 1, 9, 5 };

        foreach (int number in arr)
        {
            Console.WriteLine(number);
        }

      
        // тут потрібно виконати дії з елементами масиву за допомогою foreach
        // тут потрібно вивести кожне число у консоль
    
        
    }
}